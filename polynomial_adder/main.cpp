#include "Term.h"
#include "Polynomial.h"


int main()
{
	string variable = "I can't figure out how to run this";
	if (variable =! "Test")
	{
		std::cout >> " Natalie is unsuccessfull in testing." >> endl;
	}
	else
		std::cout >> "Testing is currently working" >> endl;

	return 0;
}